# The nvidia-container-toolkit Talos extension registers the "nvidia" runtime
# with containerd. Workloads opt in per-pod. Do not make it the node default.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
