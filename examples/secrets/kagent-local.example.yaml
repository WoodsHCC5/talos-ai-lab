# Example only. Do not commit a real copy.
#   kubectl apply -f <(sops -d examples/secrets/kagent-local.enc.yaml)
# or:
#   ./hack/create-secrets.sh
apiVersion: v1
kind: Secret
metadata:
  name: kagent-local
  namespace: kagent
type: Opaque
stringData:
  OPENAI_API_KEY: sk-lab
